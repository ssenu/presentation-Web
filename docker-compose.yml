services:
  web:
    build: .
    ports:
      - "127.0.0.1:${HOST_PORT:-8080}:8080"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/data
    restart: unless-stopped
