# 접속 비밀번호
APP_PASSWORD=password
# 세션 쿠키 서명 키. 길고 무작위한 값으로 바꾸세요. 예: python -c "import secrets;print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-long-random-string
# 호스트에서 열 포트. 127.0.0.1에만 바인딩되므로 외부 공개는 리버스 프록시로 한다.
HOST_PORT=8080
